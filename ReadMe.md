# JavaScript Basics

This repository contains JavaScript exercises and examples completed as part of my Full Stack Development learning journey.

## Topics Covered

- JavaScript Introduction
- Linking JavaScript using `<script>` and external `.js` files
- `console.log()`
- `alert()`
- Variables (`var`, `let`, `const`)
- Data Types (`string`, `number`, `boolean`, `null`, `undefined`)
- `typeof` Operator

### Operators

- Arithmetic Operators
- Assignment Operators
- Comparison (Relational) Operators
- Logical Operators
- Ternary Operator

### Conditional Statements

- `if`
- `if...else`
- Nested `if`
- `if-else` Ladder
- `switch` Statements

### Arrays

- Array Creation and Access
- `push()`
- `pop()`
- `shift()`
- `unshift()`
- `splice()`

### Dates & Time

- Date Objects
- Working with Dates
- Working with Time

### Strings

- String Methods
- `slice()`
- `substring()`
- `trim()`
- `split()`
- `includes()`
- `for...of` Loop
- Checking Email IDs using `includes()`

## Technologies Used

- HTML5
- JavaScript (ES6+)

---

This repository will be continuously updated as I learn new JavaScript concepts and build practical examples and mini projects.